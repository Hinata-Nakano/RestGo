services:
    app:
        build: . 
        image: rest-go
        volumes:
            - .:/app
            - ${gitKeyLoc}:/root/.ssh/id_rsa_git
        ports:
            - "3000:3000"
        #command: go run main.go はHTTPリクエストをリッスンしたくなったら書く
        command: sleep infinity


    
